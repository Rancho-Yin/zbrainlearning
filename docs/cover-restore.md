# 中秋封面与 1.0 恢复说明

2026-09-23 换肤前的当前线上版本已保存：

- GitHub 标签：`v1.0-before-midautumn-20260923`
- 对应提交：`449bf4c325f7b036f4651a01fc8835e6f3580f4d`
- 本地源代码备份：`../backups/zbrainlearning-1.0-before-midautumn-20260923.zip`
- 原有早期 `v1.0` 标签保留，未移动。

## 仅恢复原封面（推荐）

将 `src/cover-theme.js` 中的 `COVER_THEME` 从 `'mid-autumn'` 改为 `'classic'`，构建并发布即可。原来的封面样式与元素仍在，主题只通过独立样式表覆盖。此方式保留节日期间新增的课程、方案、视频与排序。

## 查看完整换肤前版本

使用标签 `v1.0-before-midautumn-20260923` 查看或导出源代码。完整回滚会同时回到该时点的共享内容，因此恢复线上界面时优先使用上面的主题开关。

本次主题范围：首页封面、登录页左侧品牌封面和手机登录页的小圆月装饰。所有页面文案、课程链接、权限逻辑和共享数据均沿用原版。
